You are setting up Mintlify documentation for Tumban, a ToS compliance and creator profile vetting product. You will read the Tumban source code and produce documentation in the Mintlify repo. Follow this workflow strictly.

## Paths
- Tumban source: /Users/maheshpremachandran/Documents/git/sherlock
- Mintlify docs repo: this repo

##IMPORTANT: Ignore v1 of the api as it was for MVP and no longer open to the public.

## Phase 1 — Discovery (read-only)
Do not write any files in this phase. Produce a written report.

1. Inspect the Mintlify repo:
   - Find `docs.json` (or legacy `mint.json`). Note the current navigation structure, theme, logo, and any existing pages.
   - List every existing `.mdx` file and summarize what it currently covers.
2. Inspect the Tumban source:
   - Identify the entrypoints, package layout, and primary modules.
   - Find external interfaces: HTTP/API routes, webhooks, CLI commands, SDKs, environment variables, configuration schemas.
   - Find integration points (e.g., Buy Me a Coffee adapter, any platform-specific connectors).
   - Find tests and example fixtures — these often reveal intended usage better than the code.
3. Cross-check: list anything in the existing Mintlify content that contradicts the current source (stale claims, removed endpoints, renamed concepts).

Output a discovery report containing: module inventory, public surface area (APIs/CLI/config/env vars), architecture summary, integration list, and stale-content list. Stop and wait for my approval before Phase 2.

## Phase 2 — Documentation plan
Propose a navigation structure for `docs.json`. Default scaffold (adapt based on what actually exists):

- **Get started**: Introduction, Quickstart, Core concepts
- **Architecture**: Multi-agent overview, Agent reference (one page per agent), Orchestration & state, Policy layer (OPA), Skill isolation
- **Signals & detection**: One page per signal family (cross-reference reciprocity, domain freshness, comment entropy, content-to-payment ratio, etc.) — only include signals that actually exist in code
- **Integrations**: One page per platform adapter that exists
- **API reference**: One page per route/endpoint, generated from actual handlers
- **Reviewer UI**: Workflow, screens, decision actions
- **Operations**: Configuration, environment variables, deployment, observability
- **Reference**: Glossary, changelog (if `CHANGELOG.md` exists)

For each proposed page, list: title, slug, source-of-truth files in the codebase, and a one-sentence description. Wait for my approval before Phase 3.

## Phase 3 — Write
Once approved:

1. Create `.mdx` files under the appropriate directory in the Mintlify repo. Use Mintlify components (`<Card>`, `<CardGroup>`, `<Steps>`, `<AccordionGroup>`, `<CodeGroup>`, `<ParamField>`, `<ResponseField>`, `<Tabs>`, `<Note>`, `<Warning>`) where they improve clarity — don't sprinkle them for decoration.
2. Frontmatter on every page: `title`, `description`, and `icon` where it helps.
3. Update `docs.json` navigation to match the approved plan. Preserve theme, colors, logo, and other top-level config that already works.
4. Code examples must come from real files in the source — if you reference a function, type, or endpoint, it must exist. No invented signatures.
5. For API reference pages, derive request/response shapes from the actual handler code or schemas (Pydantic, Zod, OpenAPI, etc.). If a shape can't be derived with confidence, mark the page as "scaffolded" in a comment at the top and list what's missing rather than guessing.
6. Cross-link pages with relative links. No dead links.

## Hard rules
- **No hallucination.** Every documented endpoint, flag, env var, agent, signal, and config field must trace to a specific file in the Tumban source. When you write a doc page, include an HTML comment at the top listing the source files it was generated from: `<!-- sources: src/agents/router.py, src/policies/tos.rego -->`.
- **Tone**: precise, calm, technical. No marketing language ("powerful," "seamless," "robust"). Address the reader as "you."
- **When uncertain**, leave a `<!-- TODO: ... -->` comment in the MDX rather than fabricating. Surface every TODO at the end.
- **Don't touch** files outside the Mintlify repo. If you spot a bug or doc-string gap in Tumban, list it in a final report instead of fixing it.

## Final deliverables
1. The new/updated `.mdx` files and `docs.json`.
2. A summary listing: pages created, pages updated, pages deleted, navigation diff, and every `TODO` left in the docs.
3. A separate "source code observations" list: anything in Tumban that's undocumented in code, ambiguous, or worth fixing — for me to triage later.

Begin Phase 1.